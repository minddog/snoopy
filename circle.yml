machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t snoopy .

test:
  override:
    - docker run -p 9092:9092 snoopy
    - curl --retry 10 --retry-delay 5 -v http://localhost:9092
    - nosetests
